# Gemini AI API Key (optional - for AI image generation/processing)
# Get your key from: https://aistudio.google.com/app/apikey

# For web builds (used at build time by Vite)
VITE_GEMINI_API_KEY=your_api_key_here

# For desktop builds with bundled key (used at build time by Tauri)
# Only needed if you want to bundle the API key into the desktop app
# Otherwise, users can provide their own key via the UI
GEMINI_API_KEY=your_api_key_here
