{
  "name": "zagreb-plotter",
  "version": "1.0.0",
  "description": "Zagreb iDraw 2.0 Pen Plotter Control Application (Tauri)",
  "scripts": {
    "dev": "cd frontend && npm run tauri:dev",
    "build": "cd frontend && npm run tauri:build",
    "tauri:dev": "cd frontend && npm run tauri:dev",
    "tauri:build": "cd frontend && npm run tauri:build",
    "build:frontend": "cd frontend && npm run build"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6"
  },
  "dependencies": {
    "@google/genai": "^1.34.0",
    "@serialport/parser-readline": "^13.0.0",
    "qrcode": "^1.5.4",
    "serialport": "^13.0.0"
  }
}
